# Firebase native config is provided via:
#   ios:     GoogleService-Info.plist  (project root)
#   android: google-services.json      (project root)
# Download from Firebase Console → Project Settings.
